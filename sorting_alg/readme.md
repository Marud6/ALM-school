compile with:

g++ -Iinclude -Isorting_algorithms  src/sorting_runner/sortRunner.cpp -o sort_program

g++ -Iinclude -Isorting_algorithms  src/main.cpp


run with:

./a.out > ./output/final.log